<%= form_for[@user, @product] do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_title %><br>
    <%= f.text_field :product_title %>
  </div>
  <div class="field">
    <%= f.label :product_category %><br>
  <%= f.grouped_collection_select :product_category, @shop_categories, :product_categories, :full_line, :id, :full_line %>
  </div>

  <div class="field">
    <%= f.label :product_image %><br>
    <%= f.file_field :product_image %>
  </div>

  <div class="field">
    <%= f.label :product_info %><br>
    <%= f.text_area :product_info %>
  </div>

  <div class="field">
    <%= f.label :product_price %><br>
    <%= f.text_field :product_price %>
  </div>

  <div class="field">
    <%= f.label :product_quantity %><br>
    <%= f.text_field :product_quantity %>
  </div>

  <div class="field">
    <%= f.label :product_quantity_ordered %><br>
    <%= f.text_field :product_quantity_ordered %>
  </div>        
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

